import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#3f5850',
        },
        secondary: {
            main: '#dc004e',
        },
    },
});

export default theme;
