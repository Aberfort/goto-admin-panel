# Leave empty to call the API on the same origin (used behind the nginx
# proxy in docker-compose / production). For plain `npm run dev` against a
# separately-running `php artisan serve`, set e.g.:
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
